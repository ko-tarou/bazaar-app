# 001 全体計画 — bazaar-app Go移行 + 機能拡張

## フェーズ1: アーキテクチャ移行（最優先）

### 001-1: Goバックエンド構築
- `back/` ディレクトリ作成（Go 1.24 + Gin + pgx/v5）
- DBマイグレーション（products テーブル）
- API実装: GET/POST /api/products、画像アップロード、GET /uploads/{filename}
- CORS設定（front:3000 → back:8080）

### 001-2: フロントエンド Supabase 除去
- `front/` ディレクトリに移動
- supabaseClient.ts 削除
- page.tsx を Go API fetch に書き換え
- @supabase/supabase-js 依存削除

## フェーズ2: 機能拡張（移行後に永久ループで追加）

候補:
- 価格フィールドの入力UI追加
- 商品削除機能
- 商品編集機能
- カテゴリ分類
- 検索・フィルター
- ブース管理機能
- 在庫管理
- 購入・取り置き機能
- 売上集計ダッシュボード
- QRコード生成

## 実行順

1. 001-1 Goバックエンド
2. 001-2 フロントエンド移行
3. 以降は永久ループで機能追加
